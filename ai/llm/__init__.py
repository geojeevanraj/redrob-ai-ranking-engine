"""LLM infrastructure: manager, validation, usage tracking, logging.

Note: submodules are imported directly (e.g. `from ai.llm.manager import
LLMManager`) to keep this package's import graph free of cycles.
"""
