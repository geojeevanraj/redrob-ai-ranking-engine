# Pytest config for the root-level `ai/` package tests.
# (The backend has its own config under backend/pyproject.toml.)
[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = ai/tests
addopts = -ra -q
